require: rubocop-rails

inherit_mode:
  merge:
    - Exclude

Rails:
  Enabled: true

AllCops:
  Exclude:
    - 'db/schema.rb' # this is a generated file, we don't want to be manually corrected it.
    - 'node_modules/**/*'
    - '**/*/Brewfile'

Metrics/AbcSize:
  Exclude:
    - 'db/migrate/*'
    - 'features/**/*'
    - 'spec/**/*'

Metrics/BlockLength:
  Exclude:
    - 'config/**/*'
    - 'db/migrate/*'
    - 'features/factories/*'
    - 'lib/tasks/**/*'
    - '**/*.json.jbuilder'

Metrics/ClassLength:
  Exclude:
    - 'features/**/*'
    - 'spec/**/*'

Metrics/MethodLength:
  Exclude:
    - 'db/migrate/*'
    - 'features/**/*'

Layout/LineLength:
  Exclude:
    - 'db/migrate/*'
    - 'features/**/*'

Lint/ParenthesesAsGroupedExpression:
  Exclude:
    - 'features/step_definitions/**/*'

Rails/SkipsModelValidations:
  Exclude:
    - 'features/**/*'
    - 'spec/**/*'

Rails/HasManyOrHasOneDependent:
  Enabled: false

# Migrations using `change_table` with `bulk: true` can improve some db platform migration performance
# but it also introduces some other issues as it does not support all features that add_column,
# remove_column have. Disable it and write standard, consistent migrations.
Rails/BulkChangeTable:
  Enabled: false
